# API Gateway URL
# Lưu ý: 
# - Khi chạy trên iOS Simulator/Android Emulator: có thể dùng localhost
# - Khi chạy trên thiết bị thật (iPhone/Android): cần thay localhost bằng IP của máy tính
#   Ví dụ: EXPO_PUBLIC_API_URL=http://192.168.1.7:3000/api/v1
#   Để lấy IP: chạy lệnh "ifconfig | grep 'inet ' | grep -v 127.0.0.1" trên Mac/Linux
#   hoặc "ipconfig" trên Windows và tìm IPv4 Address

EXPO_PUBLIC_API_URL=http://localhost:3000/api/v1
